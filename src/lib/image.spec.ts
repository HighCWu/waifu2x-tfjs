// import * as tf from '@tensorflow/tfjs';
import test from 'ava';

// import { Image } from './image';

// TODO(highcwu): not ready for test, need document, canvas, webgl env.
test('image', async (t) => {
  t.is(1, 1);
});
